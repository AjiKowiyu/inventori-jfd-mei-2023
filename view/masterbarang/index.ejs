
<div class="container">
    <h1>Database Barang</h1><hr>
    <div class="row">
        <div class="col-12 text-end">
            <a href="/master/barang/tambah" class="btn btn-primary">+ tambah</a>
        </div>
    </div>
    <% if (barang.length < 1) { %>
        <h3>belum ada barang</h3>
    <% } else { %>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Kode</th>
                    <th>Nama Barang</th>
                </tr>
            </thead>
            <tbody>
                <% barang.forEach( (b)=>{ %>
                    <tr>
                        <td>1</td>
                        <td><%= b.kode %></td>
                        <td><%= b.nama %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</div>